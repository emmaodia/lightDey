<h1>Editing article</h1>

<%= form_for :powerstat, url: powerstat_path(@powerstat), method: :patch do |f| %>
  <% if @powerstat.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@powerstat.errors.count, "error") %> prohibited
      this powerstat from being saved:</h2>
    <ul>
    <% @powerstat.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :location %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :address %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', powerstats_path %>
